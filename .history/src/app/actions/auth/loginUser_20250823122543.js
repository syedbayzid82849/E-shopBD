"use server";

import dbConnect from "@/lib/dbConnect";

export const loginUser = async ({ email, password }) => {
    console.log("Logging in:", { email, password });
const userCollection = dbConnect(collec)
};
